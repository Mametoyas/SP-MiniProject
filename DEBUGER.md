# บันทึกการทดสอบและ Debug — Hangman

ขอบเขต: เกมปกติ (`/hangman`) และเกม AI (`/hangman-ai?mode=assist` / `vs`)

เอกสารนี้อ้างอิงจากการอ่านโค้ดใน `games/hangman/hangman.py`, `games/hangman/hangman.js` และ `games/hangman/hangman_ai.js` รวมถึงการเตรียมทดสอบ API ด้วย Flask test client

## สรุปสิ่งที่ทดสอบ

| ลำดับ | กรณีทดสอบ | ผลที่คาดหวัง | ผล/ข้อสังเกต |
|---:|---|---|---|
| 1 | กดตัวเลข สัญลักษณ์ ช่องว่าง หรืออักษรภาษาอื่น | เกมต้องไม่รับค่าและจำนวนครั้งผิดต้องไม่เพิ่ม | หน้าเว็บกรองเฉพาะ `a-z` แล้ว แต่ API ยังรับอักษร Unicode บางตัวได้ (บัค P1) |
| 2 | กด A–Z, กดตัวเดิมซ้ำ, กดถูก และกดผิด | แสดงอักษร/จำนวนชีวิต/แป้นพิมพ์ได้ถูกต้อง | มีการป้องกันตัวเดิมซ้ำในทั้งหน้าเว็บและ API |
| 3 | โหมด VS: ผู้เล่นเดา 1 ตัว แล้วรอ AI เดา | AI เดา 1 ตัว, อัปเดตกระดาน AI และกลับมาเป็นตาผู้เล่น | ระบบเก็บการเดาของผู้เล่นกับ AI แยกกัน แม้ใช้คำเดียวกัน จึงสื่อสารกติกาไม่ชัด (P2) |
| 4 | โหมด AI ขณะ API ตอบ error/เครือข่ายขัดข้อง | แจ้งข้อผิดพลาดและกดเดาต่อได้ | ตัวแปรล็อกการกด (`guessing`) อาจค้างเป็น `true` ทำให้กดไม่ติด (P1) |
| 5 | เปิดเกมพร้อมกันสองเบราว์เซอร์ หรือผู้เล่นหนึ่งเริ่มเกมใหม่ระหว่างอีกคนเล่น | แต่ละคนต้องมีเกมของตนเอง | state ของเกมอยู่ในตัวแปร global ร่วมกันทั้ง server (P1) |
| 6 | เรียก API AI ด้วย `mode` ที่ไม่ใช่ `assist` หรือ `vs` | ควรตอบ 400 พร้อมข้อความผิดพลาด | server รับค่าอื่นได้ แต่หน้าเว็บจะพยายามอ่านข้อมูล VS ที่ไม่มี (P2) |
| 7 | รันทดสอบ API แบบอัตโนมัติในเครื่อง | Flask app ต้องเริ่มได้ | สภาพแวดล้อมนี้ import `pandas` ไม่ผ่าน เพราะขาด `python-dateutil`; ต้องติดตั้ง dependencies ก่อน จึงยังไม่ใช่ผลยืนยันบัคของเกม |

## ปัญหาที่พบและแนวทางแก้

### P1 — API ตรวจอักษรด้วย `isalpha()` ซึ่งไม่จำกัดแค่ A–Z

อาการ: เดิมเมื่อกดข้อความ/อักขระที่ไม่ใช่ A–Z เกมมีโอกาสทำงานผิดปกติ จึงมีการกรองที่หน้าเว็บด้วย `/^[a-z]$/` แล้ว ซึ่งถูกต้องสำหรับ keyboard event. อย่างไรก็ตาม API ยังใช้ `letter.isalpha()` ที่รับอักษรจากภาษาอื่นด้วย เช่น อักษรไทยหรืออักษรมีเครื่องหมาย จึงอาจถูกนับเป็นคำตอบผิดได้หากเรียก API โดยตรง หรือหากมีช่องทางอื่นส่งค่าเข้ามา

หลักฐาน:

- `games/hangman/hangman.js:228-231` และ `games/hangman/hangman_ai.js:327-330` กรอง keypress เป็น `a-z`
- แต่ `games/hangman/hangman.py:147-148` และ `games/hangman/hangman.py:226-227` ใช้ `len(letter) != 1 or not letter.isalpha()`

แนวทางแก้: ตรวจให้เป็นอักษรอังกฤษตัวพิมพ์เล็กเท่านั้นที่ฝั่ง server ด้วย เช่น `letter not in string.ascii_lowercase` หรือ `re.fullmatch(r"[a-z]", letter) is None` ทั้ง endpoint เกมปกติและ AI. หน้าเว็บควรคง regex เดิมไว้เป็นด่านแรก

### P1 — โหมด AI ล็อกการกดค้างเมื่อ API ตอบ error หรือ request ล้มเหลว

อาการ: ผู้ใช้กดตัวอักษรแล้วบางครั้งกดต่อไม่ได้ โดยเฉพาะเมื่อ server ตอบ error, เริ่มเกมใหม่จากอีกหน้าจอ หรือเครือข่ายขัดข้อง

หลักฐาน: ใน `games/hangman/hangman_ai.js:192-203` ตั้ง `guessing = true` ก่อนเรียก API แต่เมื่อได้รับ `state.error` จะ `return` ทันทีโดยไม่คืนค่า `guessing = false` และไม่เปิดปุ่มกลับมา นอกจากนี้ `fetch()` ไม่มี `try/catch/finally`; หาก request ล้มเหลว ฟังก์ชันจะจบด้วย exception และคงสถานะล็อกไว้

แนวทางแก้: ครอบการเรียก API ใน `try/catch/finally` และคืนค่า `guessing = false` ใน `finally`. กรณี error ให้เปิดปุ่มเดิมกลับมา (`btn.disabled = false`) และแสดงข้อความ เช่น “เชื่อมต่อไม่ได้ กรุณาลองอีกครั้ง”. ควรตรวจ `response.ok` ใน `apiPost()` ด้วย

### P1 — ข้อมูลเกมถูกใช้ร่วมกันทุกผู้เล่น

อาการ: หากเปิดคนละเบราว์เซอร์/Incognito หรือมีผู้ใช้พร้อมกัน การกดเดาและการเริ่มเกมใหม่ของคนหนึ่งสามารถเปลี่ยนเกมของอีกคนได้ ส่งผลให้หน้าจอเหมือนกดไม่ติด, คำเปลี่ยนเอง หรือได้รับ `not your turn`

หลักฐาน: `games/hangman/hangman.py:21-22` ประกาศ `game` และ `ai_game` เป็น dictionary ระดับโมดูล และ endpoint ทั้งหมดแก้ไข dictionary เดียวกัน (`api_new` ที่บรรทัด 215-218, `api_ai_new` ที่บรรทัด 134-136)

แนวทางแก้: เก็บ state แยกตามผู้เล่น เช่น Flask `session` พร้อม session ID, Redis/database ที่ key ด้วย session ID หรือส่ง `game_id` กลับไปให้ client. ห้ามใช้ dictionary global เป็น state ของเกมจริง

### P2 — กติกา VS AI ไม่ชัด: AI และผู้เล่นเดาคำเดียวกัน แต่รายการตัวอักษรแยกกัน

อาการ/สิ่งที่พบ: ใน VS, AI เลือกตัวอักษรแล้วผู้เล่นไม่เห็นตัวนั้นถูกเลือกบนกระดาน/keyboard ของตนทันที จึงอาจเข้าใจว่า “AI เลือกไปแล้ว เราเลือกตามไม่ได้” หรือสงสัยว่าทำไมผลการเดาไม่ถูกแชร์

หลักฐาน:

- `games/hangman/hangman.py:122-124` ตั้ง `a_word = p_word` ทำให้ทั้งสองฝั่งใช้คำเดียวกัน
- แต่บรรทัด `175` เพิ่มตัวเลือกของผู้เล่นใน `p_guessed` และบรรทัด `197` เพิ่มตัวเลือก AI ใน `a_guessed` ซึ่งเป็นคนละ list
- หน้าเว็บ VS สร้าง keyboard จาก `p.guessed` เท่านั้นที่ `games/hangman/hangman_ai.js:174`; จึงไม่สะท้อน `a.guessed`

ข้อสรุป: จากโค้ดปัจจุบัน ผู้เล่นยังส่งตัวอักษรเดียวกับที่ AI เดาได้ หากตัวนั้นยังไม่อยู่ใน `p_guessed`; แต่ตัวอักษรของ AI จะไม่ถูก “เลือกตาม” หรือเผยคำตอบบนกระดานผู้เล่น เพราะออกแบบให้ state แยกกัน นี่เป็นความคลุมเครือของกติกา/UX มากกว่าการป้องกันตัวอักษรโดยตรง

แนวทางแก้: ตัดสินใจกติกาให้ชัดเจนเพียงแบบเดียว

1. ถ้าเป็นการแข่งขันจริง: สุ่ม `a_word` คนละคำกับ `p_word` และเพิ่มข้อความว่าแต่ละฝ่ายเดาคำของตนเอง
2. ถ้าเป็นการเดาร่วมกัน: ใช้ `guessed` ชุดเดียว, อัปเดตทั้งสองกระดานหลัง AI เดา และ disable keyboard ตามตัวอักษรที่ AI เดาแล้ว

### P2 — API AI รับ `mode` ที่ไม่ถูกต้อง

อาการ: ส่ง `mode` อื่นนอกจาก `assist`/`vs` ไปที่ `/api/hangman/ai/new` แล้ว server ยังสร้างเกม แต่ response ไม่มีข้อมูล `ai`; หากหน้าเว็บพยายาม render แบบ VS จะเกิด JavaScript error

หลักฐาน: `games/hangman/hangman.py:106-107` รับ `mode` โดยไม่มี validation และสร้าง `a_word`/`ai` เฉพาะเงื่อนไข `if mode == "vs"` ที่บรรทัด 122. ขณะที่ `games/hangman/hangman_ai.js:129-133` ถือว่าค่าที่ไม่ใช่ `assist` คือ VS และเรียก `renderVS(state)`

แนวทางแก้: validate mode ใน `api_ai_new()` ทันที; หากไม่อยู่ใน `{"assist", "vs"}` ให้ตอบ `400` พร้อม `{"error": "invalid mode"}`. ฝั่ง JS ควรแสดง error แทนการ render หาก response ไม่สมบูรณ์

### P3 — การเริ่มเกมซ้ำและ response ที่มาช้าอาจทับหน้าจอเกมใหม่

อาการ: กด Play Again ซ้ำเร็ว ๆ หรือมี request เก่าที่ยังไม่ตอบ อาจทำให้ response ของเกมเก่ามา render ทับเกมที่เพิ่งเริ่ม

หลักฐาน: `games/hangman/hangman_ai.js:117-133` เรียก `initGame()` แบบ async โดยไม่มีการยกเลิก request เก่าหรือตรวจ ID ของเกม และปุ่ม Play Again เรียก `initGame(currentMode)` ซ้ำที่บรรทัด 334-339

แนวทางแก้: ปิดปุ่มระหว่างเริ่มเกม, ใช้ `AbortController` ยกเลิก request เก่า หรือเพิ่ม `game_id/request_id` แล้ว render เฉพาะ response ที่ตรงกับเกมปัจจุบัน

## เงื่อนไขก่อนทดสอบแบบรันจริง

ระหว่างเตรียม Flask test client พบว่า app import ไม่สำเร็จเนื่องจาก `pandas` ต้องใช้ `python-dateutil` แต่ environment ปัจจุบันไม่มีแพ็กเกจนี้. ให้ติดตั้ง dependencies จาก `requirements.txt` ใน virtual environment ใหม่ก่อน เช่น `pip install -r requirements.txt` แล้วจึงทดสอบ API และ UI ซ้ำ. ประเด็นนี้เป็นปัญหาการตั้งค่า environment ในเครื่องที่ทดสอบ ไม่ใช่ข้อสรุปว่า source เกมบกพร่อง

## ชุดทดสอบซ้ำหลังแก้ไข

1. เริ่มเกมใหม่ แล้วกด `a`, `A`, ตัวเลข, `!`, space, `ก` และ `é`; ยืนยันว่าเฉพาะ A–Z ที่ถูกส่งไปและค่าที่ไม่ถูกต้องไม่เปลี่ยนจำนวนชีวิต
2. กดตัวถูก/ผิด/ตัวเดิมซ้ำจนชนะและแพ้; ตรวจคำตอบ, จำนวนหัวใจ, hangman และปุ่ม Play Again
3. โหมด Assist: ตรวจ AI Hint หลังเดาถูกและเดาผิด โดยไม่มีตัวที่เดาแล้วปรากฏซ้ำ
4. โหมด VS: ตรวจตามกติกาที่เลือกไว้ (คำแยกกันหรือ guessed ร่วมกัน) และตรวจว่าตัวอักษร AI แสดง/disable ตรงตามกติกา
5. จำลอง API 400, 500 และ offline; UI ต้องปลดล็อกและแจ้งผู้ใช้ทุกกรณี
6. เปิดอย่างน้อยสอง session พร้อมกัน; การเริ่มเกมและเดาของแต่ละ session ต้องไม่กระทบกัน
